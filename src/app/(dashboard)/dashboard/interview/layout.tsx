import type { ReactNode } from "react";

export const metadata = {
  title: "Interviews | Interview AI",
  description: "Practice and review your interviews",
};

export default function InterviewLayout({ children }: { children: ReactNode }) {
  return children;
}
